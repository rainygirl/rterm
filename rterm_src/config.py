import datetime

# KST Seoul UTC+9

TIMEZONE = datetime.timezone(datetime.timedelta(hours=9))

twitter_config = {
    "consumer_key": "",
    "consumer_secret": "",
    "access_token": "",
    "access_secret": "",
}
